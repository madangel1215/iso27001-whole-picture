node_modules/
dist/
.DS_Store
*.log

# build-intermediate per-zone content (assembled into data/data.json, which IS versioned)
data/_zones/

# TEMP: token lacks `workflow` scope; re-include after `gh auth refresh -s workflow`
.github/workflows/
